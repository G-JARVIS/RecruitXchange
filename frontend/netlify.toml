[build]
  command = "npm run build"
  publish = "dist"

[dev]
  # Run the vite dev server for local Netlify Dev
  command = "npm run dev"
  port = 3000

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
